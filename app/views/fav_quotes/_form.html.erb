<%= form_for(@fav_quote) do |f| %>
  <% if @fav_quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fav_quote.errors.count, "error") %> prohibited this fav_quote from being saved:</h2>

      <ul>
      <% @fav_quote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
