<%= form_for(@personality_category) do |f| %>
  <% if @personality_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personality_category.errors.count, "error") %> prohibited this personality_category from being saved:</h2>

      <ul>
      <% @personality_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
